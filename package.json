{
  "name": "uav-test-tw",
  "version": "1.0.2",
  "private": false,
  "type": "module",
  "scripts": {
    "validate": "node tools/validate-site.mjs",
    "test": "npm run build && playwright test",
    "test:browser": "playwright test",
    "preview": "npm run build && node tools/test-server.mjs",
    "build": "node tools/build.mjs",
    "release": "npm run build && playwright install chromium && playwright test --grep-invert \"generator is reproducible\""
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.63.0",
    "cheerio": "1.2.0"
  }
}
